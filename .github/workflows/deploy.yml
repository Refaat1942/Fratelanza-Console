# Disabled — no triggers. Re-enable by adding `on:` triggers back.
name: Build & Deploy to Hostinger VPS (DISABLED)

on: {}

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
